# LILITH-CORE — рантайм-зависимости этапа 1 (скелет)
# Установка:  pip install -r requirements.txt
# Рекомендуемый способ:  pip install -e .[dev]   (ставит и проект, и pytest)
#
# Зависимости следующих этапов объявлены в pyproject.toml как extras:
#   pip install -e .[brain]     # этап 2 — мозг
#   pip install -e .[memory]    # этап 3 — память
#   pip install -e .[voice]     # этап 4 — уши и горло
#   pip install -e .[face]      # этап 5 — лицо
#   pip install -e .[hands]     # этап 6 — руки
#   pip install -e .[bridges]   # этап 7 — мосты
#   pip install -e .[stream]    # этап 8 — стрим

fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
loguru>=0.7.2
PyYAML>=6.0
python-dotenv>=1.0

# --- для тестов (этап 1) ---
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
anyio>=4.4

# --- для ручной проверки WebSocket из консоли (необязательно) ---
websockets>=12.0
