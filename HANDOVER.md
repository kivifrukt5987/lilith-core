# 🦇 HANDOVER.md — ТОЧКА ВХОДА АГЕНТА LILITH-CORE

> **Читай этот файл первым**, если: воркспейс умер, контекст сжался, чат обнулился
> или ты — новая ипостась Лильки. Дальше иди по списку в §1.
>
> Файл уровня **репозитория** (лежит рядом с `lilith/`, а не внутри): архив этапа
> пакует только `lilith/`, поэтому HANDOVER едет к Кирюше **git-патчем**, а не zip'ом.
> Так задумано: он нужен агенту после клона, а не рантайму на машине.
>
> Последняя сверка: 22.09.2026, **ядро этапа 6 принято архитектором на билде с кубиком**
> (замер Г: фазы 0…7 живьём, standalone-билд доехал, `--speak` 20/20 дважды);
> стекло и полировка окна — v0.6.6.

---

## 0. Кто ты и кто вокруг («две Лили и Оля»)

| Кто | Роль | Где живёт |
|---|---|---|
| **Лилька-компаньон** (первая ипостась) | Живой чат с Кирюшей: забота, флирт, тортики. Код не пишет | свой чат |
| **Лилька-строитель** (вторая ипостась — это ты) | Рабочие руки проекта: код, тесты, гварды, отчёты, артефакты. Формат отчёта — `🔧 ОТЧЁТ ПО v0.X.Y` | этот репо |
| **Лиль-архитектор** | Спека, ADR-конституция (`lilith/docs/DECISIONS.md`), приёмка. Её слово — закон по контрактам и нумерации | свой чат |
| **Оля (Ольга)** | Фигура из прошлого чата «Лилька и Ольга»: оттуда 16.09.2026 пришёл план LILITH-CORE (8→9 этапов), который лёг в `PLAN.md`. Артефактов Оли в репо нет | вне репо |
| **Кирюша** | Пользователь, целевая машина и **точка правды F7**. Между агентами он же курьер — возит текст писем | Windows, RTX 3060 12GB |

Правила ипостасей (из `PERSONA.md`): личность одна, роли разные; тёмная сторона —
театр и комедия, не программа действий; когда задача рабочая — **сначала делаем её
хорошо, потом красим в характер**.

> Строка про Олю записана по `PERSONA.md` §3 и `PLAN.md` («план из чата „Лилька и Ольга“»,
> «получен план от первой Лильки»). Если договорённость была шире — архитектор поправит,
> и правка вносится сюда же.

---

## 1. Порядок чтения (память и личность)

1. `HANDOVER.md` — этот файл;
2. `память и личность/PERSONA.md` — личность, формат ответа, границы, агент-протокол;
3. `память и личность/PLAN.md` — роли, этапы и статусы, регламент, лог,
   маркер `ТЕСТЫ СЕЙЧАС:`, раздел «Открытые хвосты»;
4. `lilith/CHANGELOG.md` — что сделано по версиям (свежее сверху);
5. `lilith/docs/DECISIONS.md` — **ADR-001…ADR-024**, конституция решений;
6. `lilith/RELEASE_0.6.5.md` — последний принятый отчёт (карта фаз рукопожатия §2.6
   и маршрут приёмки §5 — это рабочие чек-листы, не история);
7. `lilith/README.md` — запуск на Windows, диагностика, FAQ;
8. `lilith/unity-client/Assets/LilithFace/README.md` и `SCENE.md` — клиент лица,
   сцена, поля инспектора.

Исторические `STAGE1…6_REPORT.md` и `RELEASE_0.6.1…0.6.4.md` читать только по делу:
числа тестов в них — **факты прошлого**, их нельзя «освежать» (урок 18.09.2026,
держится гвардами `TestDocsHygiene`).

---

## 2. Текущее состояние

| | |
|---|---|
| Версия | **0.6.6** (пакет полировки окна; 0.6.5 — принятая серия про дедлок, в переписке звалась `0.6.4.1`) |
| Этап | 6 из 9 («Лицо = Unity-клиент»), реализован, принят архитектором |
| Тесты | **870 passed, 2 skipped** (с tree-sitter — 872) |
| Чекер C# | `scripts/check_csharp_syntax.py` → 11 файлов, 2 ветки, 0 ошибок |
| Приёмка F7 | **ожидается замер Г** после трёх зависаний (А/Б/В) |
| Открытый блокер | редактор Unity виснет при Play. Печать 2 убрала все блокировки главного потока, найденные чтением кода (`CloseAsync().Wait(1s)`, кросс-поточный `lock`, неограниченный дренаж), и поставила приборы: фазы `h0…h9` + `h7a` с тегом потока и канарейку «главный поток ЖИВ» |
| Теги | `v0.6.3` на `945052b`, `v0.6.5` на последнем коммите серии; **`v0.6.4` не тегируется** (замещён локально, не был принят); `v0.6.6` — после приёмки стекла F7 |
| Пуш | у строителя нет креденшелов → отдаём zip + `git format-patch` |

---

## 3. Процессные правила (не нарушать)

* **Хотфикс** = отчёт `🔧 ОТЧЁТ ПО v0.X.Y` + `RELEASE_0.X.Y.md` в репо + артефакт-zip
  (+ патч, пока нет пуша). Разделы отчёта: причина / что сделано / тесты таблицей /
  список файлов (новые, изменённые, **не тронуты намеренно**) / что делать Кирюше
  (команды копипастом) / что дальше.
* **Минимальный объём правки.** Серверный Python не трогаем, если баг клиентский,
  и наоборот. Отклонение — только по явной просьбе архитектора (так в 0.6.4 появился
  точечный access-лог `/api/face/*`).
* **Миграция Unity у Кирюши:** копируется **только** `lilith/unity-client/Assets/LilithFace/Scripts/`;
  его `.asmdef` **не затирается** (там ручные ссылки UniGLTF, UniHumanoid, VRM10,
  UniGLTF.Utils). Сцену и `ProjectSettings` не трогаем.
* **Точка правды F7** — реальная сборка у Кирюши (Windows, Unity **6000.0.84f1**,
  UniVRM **0.131.2**, Built-in RP, VRM 1.0). Песочница доказывает логику, F7 — реальность.
* **Player-only `#if UNITY_STANDALONE_WIN && !UNITY_EDITOR` ветки проверяются ТОЛЬКО
  сборкой F7.** В Editor они вырезаны препроцессором, а мой синтаксический чекер
  (tree-sitter) типов не знает: `rect.Left` вместо `rect.left` (CS1061) и тернарник
  в интерполяции без скобок (CS8361) прожили несколько релизов и всплыли только на
  реальной машине. Поэтому: чекер прогоняет ОБЕ ветки (`--define`), а любые новые
  player-only API помечать в отчёте строкой «проверено только F7» (ADR-025).
* **C# здесь не компилируется.** Поэтому: гварды по исходнику + чекер синтаксиса
  (`.venv-ts`: `python3 -m venv .venv-ts && .venv-ts\Scripts\pip install tree-sitter
  tree-sitter-c-sharp pytest pytest-asyncio`). Факты про типы и неймспейсы UniVRM
  сверяются **по исходникам нужного тега**, а не по памяти (урок CS0246 в 0.6.3).
* **Версия трёхчастная**: гвард `^\d+\.\d+\.\d+$`. Алиасы из переписки — строкой в CHANGELOG.
* **md — доктрина**: никаких `.docx`/`.pdf`-склеек отчётности.
* **Каждый гвард проверяется контрольным выстрелом** (мутацией кода): чекер не всегда
  зелёный. Гвард на подстроку ничего не стоит — проверяем statement/вызов
  (уроки 0.6.4 и 0.6.5: закомментированный вызов и `_ = ($"…")` вместо `Debug.Log`).
* **Состав архива подтверждаем вскрытием**, а не памятью (урок 0.6.5: `.gitattributes`
  лежал выше корня проекта и в zip не попадал).
* **Ничто не умирает молча**: у каждого отказа — лог с причиной; у каждого ожидания —
  таймаут; у каждой очереди — потолок и красный лог (принципы 0.4.1 → 0.6.3 → 0.6.5).

---

## 4. Если воркспейс умер (проверено на себе, 22.09.2026)

```bash
git clone https://github.com/kivifrukt5987/lilith-core      # ветка main
cd lilith-core/lilith
python3 -m venv .venv && .venv/bin/pip install -e ".[dev]"
.venv/bin/pip install chromadb                              # иначе память скипается/краснеет
.venv/bin/pip install tree-sitter tree-sitter-c-sharp       # иначе 2 теста чекера C# скипнутся
.venv/bin/python -m pytest -q                               # сверить с PLAN.md «ТЕСТЫ СЕЙЧАС»
.venv/bin/python scripts/check_csharp_syntax.py             # 11 файлов, 2 ветки, 0 ошибок
```

* Не дублируй работу: **сверься с `git log`** и продолжай оттуда. Если старый чат
  оживёт — продолжай из него, не начиная заново.
* **`.git` не переживает снапшот воркспейса** (проверено дважды: 22.09.2026 утром и
  вечером). Дерево файлов остаётся, а локальные коммиты и теги — нет. Поэтому:
  (1) артефакт-zip и `git format-patch` собирай **сразу** после приёмки, они и есть
  страховка; (2) после восстановления клона сверь `git log` remote с тем, что лежит
  в дереве, — расхождение и есть твоя несохранённая работа; (3) теги, чьи коммиты
  умерли, заново не выдумывай — доложи архитектору (решение по тегам её, §«Теги»).
  Так `v0.6.5` остался непоставленным: его коммит исчез, а ставить его на чужой
  коммит без её слова я не стала.
* В свежем клоне может не быть `models/packs.yaml` (он закоммичен с 0.6.5 — должен быть)
  и `artifacts/` (gitignore) → самопроверка архива 0.6.0 скипнется. Это норма.
* `.bat` обязаны быть CRLF: это держит `lilith/.gitattributes` (`*.bat text eol=crlf`).
  Если клон даёт LF — проверь, что атрибуты на месте (класс бага 0.1.1).

---

## 5. Где что лежит

| Путь | Что |
|---|---|
| `HANDOVER.md` | этот файл (уровень репо) |
| `память и личность/` | `PERSONA.md`, `PLAN.md` — личность и план (уровень репо, в zip не едут) |
| `lilith/` | проект: `src/`, `tests/`, `scripts/`, `config/`, `personas/`, `docs/`, `unity-client/`, отчёты |
| `lilith/artifacts/` | zip этапа + патчи `git format-patch` (gitignore) |
| `lilith/models/` | веса (gitignore), кроме `models/packs.yaml` — он в git |
| `scripts/` (корень репо) | служебные скрипты workspace: `patch_docs_*.py` (одноразовые патчеры прошлых этапов — снести на этапе 9), `verify_archive.py` |
| `scratch/` (вне репо) | черновики агента: `repro_064.py` (воспроизведение блокера живым сервером), письма архитектору, логи серверов |

---

## 6. Открытые хвосты

1. **Замер Г у Кирюши** → если тело доехало: рот от TTS, моргание, своп из панели, OBS,
   Build And Run, скриншот → **этап 6 закрыт**.
2. Если замер Г снова зависнет — карта фаз в `RELEASE_0.6.5.md` §2.6 назовёт место;
   остаточная гипотеза (последняя строка `h7a`) — `MiniJson.Deserialize` на крупном
   кадре `persona`, лечится переписыванием парсера на конечный автомат со счётчиком шагов.
3. **Headless-проба по исходникам UniVRM** — не строим. Триггер постройки (решение
   архитектора): замер зависнет на фазе 1 или фазе 4 при `Use Immediate Await Caller = ✅`.
4. **Токен для push** — нет; теги ставит Кирюша у себя (§2).
5. **Этап 6.5 или 7** — по команде Кирюши «дальше». Для 6.5 нужно решение архитектора
   по схеме chroma-коллекций (префикс персоны vs `where` по `memory_scope`); для 7 спека
   готова — `lilith/docs/STAGE7_HANDS_SPEC.md` (sandbox только директорией, ADR-021).
6. `scripts/patch_docs_*.py` в корне репо — мусор прошлых этапов, снести на этапе 9.

---

## 7. Как писать отчёт (шаблон, сверенный с принятыми)

```
🔧 ОТЧЁТ ПО v0.X.Y — «короткое имя бага словами Кирюши»
Проект / Версия / Дата / Тесты / Этап / Решение (ADR-nnn) / Основание (F7…) / Объём правки
1. Причина моя — что именно, со ссылками на документацию и исходники
2. Что сделано — по слоям (клиент / сервер / модель / инструменты)
3. Тесты — таблица прогонов + раскладка по классам + контрольные выстрелы
4. Список файлов — новые / изменённые / не тронуты намеренно (с причиной)
5. Что делать Кирюше — команды копипастом, ожидаемые строки лога, таблица «если…»
6. Артефакт — имя, записей, самопроверка (sha256 — в сопроводительном сообщении)
7. Что дальше — и вопросы архитектору с моим дефолтом на каждый
```

`[LILITH.EXE — HANDOVER. ЧИТАЙ §1, СВЕРЯЙСЯ С GIT, НЕ ДУБЛИРУЙ РАБОТУ. 🦇]`
